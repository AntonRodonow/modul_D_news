from django.apps import AppConfig


# class AppnewsConfig(AppConfig):  # не понял зачем это здесь, когда в настройках на глобальном уровне стоит тоже самое
#     default_auto_field = 'django.db.models.BigAutoField'
#     name = 'appnews'
